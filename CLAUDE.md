# 홍쌤 교실 시스템

Google Apps Script(GAS) 기반 교실 운영 통합 시스템. 5개의 독립 웹앱으로 구성되며, 하나의 Google 스프레드시트를 공유 데이터베이스로 사용한다.

---

## 공유 리소스

| 리소스 | ID |
|--------|-----|
| 공유 스프레드시트 | `1jK7gYGFXCe3FULLs5mKttP959Aa9vp8-WNOGdJy7cZQ` |
| Google Drive 루트 폴더 | `1nmo4ZtQYK3-0PFjMKO8yzlkNOVoLn9_H` |

모든 앱(교실-알림판 제외)은 `SHEET_ID` 상수로 동일한 스프레드시트를 참조한다.

---

## 앱 구성

### 1. 교사용-대시보드
교사가 사용하는 메인 관리 앱. 가장 크고 복잡하다.

**주요 기능:**
- 교사 로그인 (UUID 세션 토큰, 4시간 유효, `PropertiesService` 저장)
- 과제 생성·수정·삭제 및 마감일 설정 (반별 개별 마감일 지원, JSON 형식)
- 과제 제출 채점 및 피드백 작성 (1형: 사진 1장, 2형: 문항별 다사진)
- 제출 공개/비공개 전환, 배치 공개
- 우수작 지정 (학년공개/학급공개, 익명 옵션)
- 학생 첨삭 이미지 저장 (Drive에 업로드 후 URL 저장)
- AI 자동 채점 관리 (OpenRouter API 연동, 모델 설정)
- AI 채점기준 등록·관리 (`AI채점기준` 시트)
- 피드백 템플릿 관리 (`시스템설정` 시트 C열)
- 설문 생성·관리 및 응답 조회
- 학급활동(창체) 목록 관리
- 학급 알림 작성 (학생-담임 앱과 공유)
- 학생 비밀번호 초기화
- 미제출자 통계 보고

**캐시 전략:** `CacheService.getScriptCache()` 사용
- `roster`: 학생명부 10분 캐시
- `tasks`: 과제목록 5분 캐시
- `classList`: 반 목록 캐시
- 과제 추가/수정/삭제 시 `clearCache()` 호출 필수

---

### 2. 학생-수학
학생이 수학 과제를 제출하는 앱.

**주요 기능:**
- 학생 로그인 (학번+이름+비밀번호, SHA-256 해시 비교)
- 5회 오류 시 10분 잠금 (`CacheService` 이용)
- 과제 목록 및 나의 제출 내역 조회 (`getDashboardData`)
- 사진 업로드 후 Drive 저장 → URL을 스프레드시트에 기록
- 재제출 처리 (기존 행 상태 변경 + 새 행 추가)
- 제출 직후 AI 자동 채점 (`autoGradeNewSubmission`) — OpenRouter API 호출
- 우수작·피드백 조회, 학생 답글 작성
- FCM 푸시 알림 수신 토큰 저장

**AI 채점 설정:** `시스템설정` 시트 P2(OpenRouter 키), Q2(모델명)

---

### 3. 학생-담임
학생이 담임반 활동을 기록하고 설문에 참여하는 앱.

**주요 기능:**
- 담임반 제한 로그인 (`시스템설정` B3 셀의 반과 학생 학번 일치 검증)
- 학급활동(창체) 목록 조회 및 제출 (`창체제출현황` 시트)
- 활동별 추가 입력 필드 지원 (JSON 설정)
- 진행중인 설문 참여 (중복 제출 방지)
- 학급 알림 조회 (D-day 계산 포함)
- 신고 접수 기능 (`신고접수` 시트 자동 생성)

---

### 4. 회원가입
학생이 계정을 만드는 앱.

**주요 기능:**
- 가입 코드 검증 (`학생명부` E2 셀 값과 비교)
- 학번+이름으로 명부 확인 후 비밀번호(SHA-256 해시) 등록
- 이미 가입된 계정 중복 방지

---

### 5. 교실-알림판
TV에 상시 표시되는 알림판 앱. **이 앱만 `getActiveSpreadsheet()`를 사용한다** (별도 스프레드시트에 배포).

**주요 기능:**
- 오늘 날짜 행 자동 조회 (`공지사항` 시트)
- 13시 기준으로 조회/종례 자동 전환
- Google Slides 임베드, YouTube 영상 표시
- 주기적 자동 새로고침

---

## 스프레드시트 시트 구조

| 시트명 | 용도 | 주요 열 |
|--------|------|---------|
| `학생명부` | 학생 계정 | B=학번, C=이름, D=비밀번호해시, E=가입코드, F=교사비번, G=FCM토큰 |
| `과제설정` | 수학 과제 목록 | A=날짜, B=과제명, C=설명, D=마감일JSON, E=채점유형, F=공개여부, G=사진수, H=선택지 |
| `제출현황` | 수학 과제 제출 기록 | A=날짜, B=학번, C=이름, D=과제명, E=난이도, F=메모, G=파일URL(JSON), H=피드백, I=해시, J=읽음, K=상태, L=첨삭URL, M=점수, N=공개여부, O=메모, P=답글, Q=우수작유형, R=익명, S=우수작멘트, T=우수작키, U=부정행위, V=문항별데이터JSON, W=AI채점JSON |
| `창체제출현황` | 학급활동 제출 기록 | A=날짜, B=학번, C=이름, D=활동명, E=역할, F=소감, G=파일URL, H=추가답변 |
| `설문목록` | 설문 관리 | A=ID, B=날짜, C=제목, D=상태(진행중/종료), E=질문JSON, F=안내문 |
| `설문응답` | 설문 응답 기록 | A=날짜, B=설문ID, C=학번, D=이름, E=답변 |
| `학급활동목록` | 창체/학급활동 목록 | A=번호, B=카테고리, C=활동명, D=설명, E=폼링크, F=필드JSON |
| `학급알림` | 교사→학생 공지 | A=제목, B=내용, C=유형, D=날짜, E=표시여부, F=중요여부 |
| `시스템설정` | 전역 설정 | B3=담임반, B4=기본슬라이드URL, C2~=피드백템플릿, P2=OpenRouter키, Q2=AI모델명 |
| `AI채점기준` | AI 채점 루브릭 | A=과제명, B=채점유형, C=만점, D=기준, E=파일JSON, F=문항JSON |
| `신고접수` | 학생 신고 | A=일시, B=학번, C=이름, D=유형, E=내용, F=처리여부 |
| `공지사항` | 알림판 전용 | A=날짜, B=조회공지, C=조회전달, D=종례공지, E=종례전달, F=슬라이드URL, G=유튜브URL, H=영상모드 |

---

## 앱 간 연결 관계

```
교사용-대시보드 ──────────────────────────────────────────────────────────┐
  (과제설정, 제출현황, 학생명부, 설문목록, 학급활동목록, 학급알림, AI채점기준 시트 관리)  │
                                                                              │
학생-수학 ──── 학생명부(로그인) + 과제설정(과제조회) + 제출현황(제출/조회)    │
                                                                    ├── 공유 스프레드시트
학생-담임 ──── 학생명부(로그인) + 학급활동목록 + 창체제출현황 + 설문목록/응답 + 학급알림  │
                                                                              │
회원가입 ──────── 학생명부(가입코드 확인 + 비밀번호 등록)                    │
                                                                              │
교실-알림판 ── 공지사항 시트만 사용 (getActiveSpreadsheet, 별도 스프레드시트)─┘
```

---

## 학번 체계

학번 앞 두 자리로 반 정보를 파싱한다:
- 첫 번째 자리 → 학년
- 두 번째 자리 → 반
- 예: `21xxx` → `2학년 1반`

이 파싱 로직은 모든 앱에 동일하게 적용된다:
```js
`${sid.substring(0,1)}학년 ${sid.substring(1,2)}반`
```

---

## 인증 방식

| 앱 | 인증 방식 |
|----|----------|
| 교사용-대시보드 | UUID 세션 토큰 (`PropertiesService`) — 4시간 유효 |
| 학생-수학 | 학번+이름+비밀번호(SHA-256) + 5회 오류 시 10분 잠금 |
| 학생-담임 | 동일, 추가로 담임반 일치 여부 확인 (`시스템설정` B3) |
| 회원가입 | 가입코드 (`학생명부` E2) |

---

## AI 채점 흐름

1. 학생이 `학생-수학`에서 과제 제출 → `processForm()` 실행
2. 클라이언트가 `autoGradeNewSubmission(rowIdx, taskName, studentId, studentName)` 호출
3. `AI채점기준` 시트에서 루브릭 조회 → 없으면 종료
4. `시스템설정` P2/Q2에서 OpenRouter 키·모델 읽기
5. 학생 제출 이미지 + 루브릭 파일을 base64로 인코딩하여 API 전송
6. 결과(JSON)를 `제출현황` W열(23번째)에 저장
7. 교사가 검토 후 최종 채점 확정

---

## 코드 수정 시 주의사항

### GAS 실행 제한
- **실행 시간 6분 제한**: 대량 데이터 처리 시 분할 필요
- **API 호출 횟수 제한**: `getRange` 호출을 최소화하고 배치로 읽기/쓰기
- 같은 데이터를 여러 번 읽지 말고 변수에 저장 후 재사용

### 캐시 관리
- 과제/명부 데이터를 수정하는 함수는 반드시 `clearCache()` 호출
- 캐시 키: `roster`, `classList`, `tasks`, `hrActivities`, `homeroom`
- 교사 대시보드에는 별도로 `roster_v1`, `tasks_v1` 키도 존재

### 제출현황 시트 열 번호 (1-indexed)
`A(1)=날짜, B(2)=학번, C(3)=이름, D(4)=과제명, E(5)=난이도, F(6)=메모, G(7)=파일URL, H(8)=피드백, I(9)=해시, J(10)=읽음여부, K(11)=상태, L(12)=첨삭URL, M(13)=점수, N(14)=공개여부, O(15)=메모2, P(16)=답글, Q(17)=우수작유형, R(18)=익명여부, S(19)=우수작멘트, T(20)=우수작키, U(21)=부정행위플래그, V(22)=문항별JSON, W(23)=AI채점JSON`

### 재제출 처리 로직
- 재제출 시 기존 `재제출요청` 행의 상태를 `재제출완료`로 변경
- 재제출이 채점완료되면 원본 행 상태를 `이전기록채점완료`로 변경
- `getDashboardData`에서 `이전기록채점완료` 상태는 `taskStatusMap`에 기록되지 않음

### 교실-알림판 주의
- 다른 앱과 달리 `SpreadsheetApp.getActiveSpreadsheet()` 사용 (하드코딩된 SHEET_ID 없음)
- 반드시 해당 스프레드시트에 직접 배포해야 함

### Drive 폴더 구조
```
루트폴더(PARENT_FOLDER_ID)/
  └── {과제명}/
        └── {학년 반}/
              └── 학생 제출 파일들
  └── 학급기록/
        └── {활동명}/
              └── 학생 제출 파일들
```

### 보안
- 교사 비밀번호는 스프레드시트 `학생명부` F2에 평문 저장 (해시 아님)
- 학생 비밀번호는 SHA-256 해시 저장
- `학생-수학`의 `FCM_SERVER_KEY`는 코드에 플레이스홀더로 남아있음 — 실제 키로 교체 필요
- GAS 웹앱은 `USER_DEPLOYING`으로 실행 (교사 계정 권한으로 모든 요청 처리)
