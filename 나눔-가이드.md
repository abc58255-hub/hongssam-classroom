# 홍쌤 교실 시스템 — 나눔 가이드

이 시스템은 하드코딩된 ID가 없어서, 코드를 그대로 복사해 가면 **자기 계정의 스프레드시트로 바로 시작**할 수 있습니다.

---

## 받는 분 시작하기 (순서대로)

### 1. 교사용-대시보드부터 설치
1. GAS 프로젝트 사본 만들기 (또는 새 프로젝트에 `Code.js`, `index.html`, `setup.html` 붙여넣기)
2. 배포 → 새 배포 → 웹 앱
   - 실행 사용자: **나(소유자)**
   - 액세스 권한: **모든 사용자** (자체 비밀번호 로그인이 있음)
3. 배포된 웹 앱 열기 → **초기 설정 화면**이 자동으로 뜸
4. **"🆕 새로 시작하기"** 선택 → 교사 비밀번호(필수), 담임반·가입코드(선택) 입력
   - 데이터 스프레드시트(전체 시트 구조)와 드라이브 폴더가 **자동 생성**됩니다
5. 대시보드에 한 번 로그인하면 다른 앱들이 자동 연결에 사용할 **연결 파일**이 드라이브에 만들어집니다

### 2. 나머지 앱 설치
앱마다 똑같이: 사본 만들기 → 웹 앱 배포 → 열기. **끝!**

대시보드에서 한 번 연결(또는 새로 시작하기)하면, 나머지 앱들은 드라이브에 남은 연결 파일(`홍쌤교실시스템_SHEET_ID`)을 읽어 **자동으로 같은 스프레드시트에 연결**됩니다.
- 처음 열 때 권한 승인 화면이 뜨면 승인해주세요 (소유자 계정으로 1회)
- 혹시 초기 설정 화면이 보이면: 새로고침 한 번 → 그래도 보이면 1번에서 만든 스프레드시트 URL을 직접 붙여넣으면 됩니다

| 앱 | 배포 액세스 권한 권장 | 비고 |
|----|----------------------|------|
| 학생-수학 | 모든 사용자 | 학생 로그인 자체 인증 |
| 학생-담임 | 모든 사용자 | 〃 |
| 회원가입 | 모든 사용자 | 가입코드로 보호 |
| 도장-입력 | 모든 사용자 | 교사 비밀번호 잠금(1시간 세션) 내장 |
| 진도표 / 우리반-설문 / 교실-칭찬 / 도장-데이터 | **나만** | 교사 전용 |
| 교실-알림판 | 모든 사용자 | ⚠️ 아래 별도 안내 |

> 실행 사용자는 모든 앱에서 **나(소유자)** 로 설정하세요.

### 3. 바로가기 등록
교사용-대시보드 좌측 하단 **⚙️(바로가기 URL 설정)** 에서 각 앱의 배포 URL을 등록하세요.
메뉴의 `진도표 관리`, `칭찬 관리` 같은 외부 링크가 여기 등록된 주소로 열립니다.
(등록 전에 클릭하면 URL 입력창이 떠서 그 자리에서 저장할 수도 있어요.)

### 4. 교실-알림판 (선택)
이 앱만 구조가 다릅니다. `공지사항` 시트가 있는 **별도 스프레드시트**에서
확장 프로그램 → Apps Script로 **시트에 바인딩된 프로젝트**를 만들어 코드를 붙여넣고 배포하세요.

### 5. 푸시 알림 — FCM (선택)
설정하지 않아도 시스템은 정상 동작합니다(알림만 발송 안 됨). 사용하려면:
1. Firebase 프로젝트 생성 → 웹 앱 등록
2. `fcm-sw/` 폴더를 GitHub Pages 등에 호스팅하고 `fcm-sw/config.js`에 Firebase 웹 설정 입력
3. Firebase 콘솔 → 프로젝트 설정 → 서비스 계정 → 새 비공개 키 생성
4. 시스템설정 시트에 키 입력:

| 키 | 값 |
|----|----|
| `FCM_PROJECT_ID` | Firebase 프로젝트 ID |
| `FCM_CLIENT_EMAIL` | 서비스 계정 이메일 |
| `FCM_PRIVATE_KEY` | 서비스 계정 비공개 키 |
| `FCM_REGISTER_URL` | 호스팅한 register.html 주소 |
| `알림아이콘URL` | 알림 아이콘 이미지 주소 (선택) |

5. 교사용-대시보드 GAS 에디터에서 `installAutoNotifyTriggers` 1회 실행 (자동 알림 트리거 설치)

### 6. AI 자동 채점 — OpenRouter (선택)
시스템설정 시트에 `OpenRouter키`, `AI모델명` 입력. 미설정 시 AI 채점만 건너뜁니다.

---

## 시스템설정 시트 키 목록

| 키 | 용도 | 필수 |
|----|------|------|
| `교사비밀번호` | 대시보드 로그인 + 도장-입력 잠금 | ✅ (새로 시작하기에서 자동 입력) |
| `드라이브폴더ID` | 제출 파일 저장 폴더 | 자동 생성 |
| `담임반` | 학생-담임 로그인 제한 (예: `2학년 1반`) | 담임 기능 사용 시 |
| `가입코드` | 회원가입 검증 코드 | 회원가입 사용 시 |
| `바로가기_수학교실` 등 `바로가기_*` | 대시보드 메뉴/푸시 클릭 링크 | 권장 |
| `FCM_*`, `알림아이콘URL` | 푸시 알림 | 선택 |
| `OpenRouter키`, `AI모델명` | AI 자동 채점 | 선택 |
| `기본슬라이드URL` | 알림판 기본 슬라이드 | 선택 |

---

## 설정을 바꾸고 싶을 때

- **연결된 스프레드시트 변경**: GAS 에디터 → 프로젝트 설정 → 스크립트 속성 → `SHEET_ID` 삭제 → 웹 앱을 다시 열면 초기 설정 화면이 다시 나타남
- **주의**: 학생이 쓰는 공개 앱은 SHEET_ID가 비어 있는 동안 누구나 설정 화면을 볼 수 있으니, 삭제 후 바로 다시 설정하세요
